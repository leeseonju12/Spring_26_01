<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lsj.demo.repository.ArticleRepository">
	<insert id="writeArticle" parameterType="map">
		INSERT INTO article
		SET
		regDate = NOW(),
		updateDate = NOW(),
		boardId = #{boardId},
		memberId = #{memberId},
		title = #{title},
		body = #{body}
	</insert>

	<select id="getLastInsertId" parameterType="int">
		SELECT
		LAST_INSERT_ID();
	</select>

	<delete id="deleteArticle" parameterType="int">
		DELETE FROM article
		WHERE id = #{id}
	</delete>

	<update id="modifyArticle" parameterType="map">
		UPDATE article
		<set>
			<if test="title != null and title != ''">title = #{title},</if>
			<if test="body != null and body != ''">`body` = #{body},</if>
			updateDate = NOW()
		</set>
		WHERE id = #{id}
	</update>

	<select id="getForPrintArticle" parameterType="int"
		resultType="com.lsj.demo.vo.Article">
		SELECT A.*, M.nickname AS extra__writer, B.code AS extra__boardCode
		FROM article AS A
		INNER JOIN `member` AS M
		ON A.memberId = M.id
		INNER JOIN board AS B
		ON A.boardId = B.id
		WHERE A.id = #{id}
	</select>

	<select id="getArticleById" parameterType="int"
		resultType="com.lsj.demo.vo.Article">
		SELECT *
		FROM article
		WHERE id =
		#{id}
	</select>

	<select id="getForPrintArticles"
		resultType="com.lsj.demo.vo.Article">
		SELECT A.*, M.nickname AS extra__writer
		FROM article AS A
		INNER JOIN `member` AS M
		ON A.memberId = M.id
		WHERE boardId = #{boardId}
		ORDER BY A.id
		DESC
	</select>

	<select id="getArticles"
		resultType="com.lsj.demo.vo.Article">
		SELECT A.*, M.nickname AS extra__writer
		FROM article AS A
		INNER JOIN `member` AS M
		ON A.memberId = M.id
		ORDER BY A.id
		DESC
	</select>

</mapper>