<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lsj.demo.repository.BoardRepository">

	<select id="getBoardById" parameterType="int"
		resultType="com.lsj.demo.vo.Board">
		SELECT *
		FROM board
		WHERE id = #{id} AND delStatus = 0
	</select>

<select id="getForListupBoard" parameterType="int"
    resultType="com.lsj.demo.vo.Article">
    SELECT A.*, M.nickname AS extra__writer
    FROM article AS A
    INNER JOIN `member` AS M
    ON A.memberId = M.id
    WHERE A.boardId = #{boardId}
    ORDER BY A.id DESC
</select>

</mapper>